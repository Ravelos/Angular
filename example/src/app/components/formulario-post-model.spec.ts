import { FormularioPostModel } from './formulario-post-model';

describe('FormularioPostModel', () => {
  it('should create an instance', () => {
    expect(new FormularioPostModel()).toBeTruthy();
  });
});
